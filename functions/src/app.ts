// import dotenv from "dotenv";

// import { app } from "@entry-points/api/service";

// dotenv.config();
// const port = process.env.PORT;


// app.listen(port, () => console.log('Server run on port: ', port));
